import { getImageUrl } from "@utils/images";

# Bandit Level 0

The task in this level is to simply connect to `bandit.labs.overthewire.org`, with user `bandit0`, on port `2220`. The password is `bandit0`.

To do this, we use the `ssh` command.

## Solution

`ssh -p 2220 bandit0@bandit.labs.overthewire.org`.

<div className="halfgrid">
	<div className="halfgriditem emphasis4 dark card">
		<img className="articleImage" src={getImageUrl("learning/cybersecurity/bandit/level0/term1.png")} alt="" ></img>
	</div>
	<div className="halfgriditem emphasis4 dark card">
		<img className="articleImage" src={getImageUrl("learning/cybersecurity/bandit/level0/term2.png")} alt="" ></img>
	</div>
</div>
